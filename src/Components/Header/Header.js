import styles from './Header.module.scss';

const Header = () => {
    return(
        <div className="header">
            <h2 className={styles.h2}>Scrabbler</h2>
            <h4 className={styles.h4}>Image goes here later...</h4>
        </div>
    )
}

export default Header;